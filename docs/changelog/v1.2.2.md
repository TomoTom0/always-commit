# v1.2.2 (2026-07-04)

## New Features

（変更内容をここに記載）

## Bug Fixes

- `alcom finish` / `alcom save` が `git commit-tree`（plumbing）を使用するため `commit.gpgsign=true` 設定が無視され、署名設定を有効化していてもコミットが署名されない問題を修正。`commit.gpgsign` を検出し `-S` フラグを付与するようにした (#28)

## Changes

（変更内容をここに記載）

## Performance

（変更内容をここに記載）

## Refactoring

（変更内容をここに記載）

## Repository Management

（変更内容をここに記載）

## Internal Improvements

- `verify_redo` / `verify_output_improvements` テストに60000msのタイムアウトを明示し、CI環境でのテスト安定性を向上 (#29)

## Known Issues

（変更内容をここに記載）
