# v1.2.4 (2026-07-24)

## Internal Improvements

- 開発用グローバルインストール（`install:global`）を `npm install -g .` から `pnpm run build && pnpm link --global` に変更し、プロジェクトの pnpm 運用と統一。README の開発用セットアップ手順も pnpm に合わせて更新 (#36)
