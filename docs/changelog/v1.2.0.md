# v1.2.0 (2026-05-25)

## New Features

- redoコマンドの実装: undoで取り消したスナップショットを1つ復元 (TASK-28)

## Bug Fixes

- ALCOM_ALLOW=false時でもhelp/docs/versionコマンドを実行可能に修正 (TASK-20)
- parseIntの基数に10を指定し、予期しない解析を防止 (TASK-21)
- .envファイルのALCOM_ALLOW解析でスペース、export接頭辞、行末コメントに対応 (TASK-23)
- setup --projectでprocess.cwd()ではなくgetGitRoot()を使用するよう修正 (TASK-24)
- helpコマンドの許可判定をactionCommand === thisCommandに変更し、引数なし実行時や--version時のブロックを修正 (TASK-25)
- redoコマンドの操作順序を修正（resetHard成功後にpopUndoStackを実行） (TASK-30)
- changelogのcheckout hook記述を正確な内容に修正 (TASK-19)

## Changes

- undoコマンドの出力に取り消したスナップショットのメッセージ、残りスナップショット数、redoによる復元案内を追加 (TASK-27)
- logコマンドの表示を降順（新しい順）に変更し、UTCからローカル時刻に変更 (TASK-22)
- checkout hookメッセージをfinish優先からundo優先フローに変更（alcom log確認→不要分だけundo→残りをfinishの流れを案内） (TASK-29)
