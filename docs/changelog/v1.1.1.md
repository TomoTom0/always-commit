# v1.1.1 (2026-05-16)

## Fix

- `PreToolUse` hook の正規表現を修正（`git checkout -b/-B` がブロック対象外になっていた）
- `alcom docs` 一覧でディレクトリ以外のエントリをトピックとして抽出する問題を修正
- `alcom docs` 一覧で .md ファイル0件のトピックが空括弧で表示される問題を修正
- `findBaseCommit` のコミット数上限（500件）を撤廃

## Other

- テストランナーを Bun から vitest に移行、ビルドを esbuild に統一（bun依存を完全に除去）
